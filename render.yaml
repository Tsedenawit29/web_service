services:
  - type: web
    name: loan-management-system
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/loan-management-system-1.0.0.jar
    envVars:
      - key: PORT
        value: 8080
      - key: JAVA_VERSION
        value: 21

